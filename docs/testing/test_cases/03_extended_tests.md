# 扩展与边界测试用例 (Extended & Edge Cases)

本文件包含 E001-F057 共 57 条扩展与边界用例，重点关注性能、稳定性、兼容性及极端场景。
**优先级**：中 (P2)
**执行阶段**：专项测试、回归测试

## 1. 性能与稳定性 (E001-E006)

| 用例ID | 模块 | 用例标题 | 预期结果 |
| :--- | :--- | :--- | :--- |
| E001 | 性能 | 会话学习连续操作100次无泄漏 | 内存占用稳定，响应时间无明显增加 |
| E002 | 性能 | 今日计划查询并发20次稳定 | 接口响应正常，无死锁 |
| E003 | 性能 | 视频生成并发队列串行化（用户级） | 同一用户同一时间只能有一个生成任务 |
| E004 | 性能 | 稳定乱序在大词书（>10k）性能 | 乱序算法耗时在可接受范围 (<100ms) |
| E005 | 稳定性 | Redis失联fallback仍可学习 | 自动降级使用内存存储，不影响核心流程 |
| E006 | 稳定性 | 数据库短暂失败重试策略（如有） | 瞬时网络抖动不报错 |

## 2. 兼容性与容错 (E007-E018)

| 用例ID | 模块 | 用例标题 | 预期结果 |
| :--- | :--- | :--- | :--- |
| E007 | 兼容性 | Windows路径分隔符处理正确 | 路径拼接使用 path.join，无硬编码 '/' |
| E008 | 兼容性 | Linux/Mac路径分隔符处理正确 | 路径拼接适配 POSIX 标准 |
| E009 | 兼容性 | VITE_API_BASE_URL不同格式兼容 | 支持带/不带末尾斜杠的配置 |
| E010 | 容错 | startSession缺bookTagId兼容 | 使用用户上次选择或默认词书 |
| E011 | 容错 | 例句translation缺失处理 | 前端不显示翻译，不报错 |
| E012 | 容错 | pronunciations缺失处理 | 前端不显示音标，不报错 |
| E013 | 容错 | mastery_focus缺失默认recognition | 默认为识别题 |
| E014 | 容错 | shuffle salt缺失自动创建 | 自动生成新 salt 并保存 |
| E015 | 容错 | 视频脚本缺失返回失败任务 | Job 状态标记为 failed，记录错误信息 |
| E016 | 容错 | Python命令不可用返回失败任务 | Job 状态标记为 failed |
| E017 | 容错 | 脚本RAW日志无法解析的兼容 | 忽略非 JSON 日志，不影响流程 |
| E018 | 容错 | 清理任务文件不存在容错 | 仅更新数据库状态，不抛出异常 |

## 3. 安全与前端边界 (E019-E034)

| 用例ID | 模块 | 用例标题 | 预期结果 |
| :--- | :--- | :--- | :--- |
| E019 | 安全 | JWT泄露场景撤销（若实现） | (若支持黑名单) 令牌失效 |
| E020 | 安全 | CORS跨源限制校验 | 非法 Origin 请求被拒绝 |
| E021 | 安全 | XSS/输入内容在前端组件显示安全 | 单词/例句内容转义显示 |
| E022 | 安全 | SQL注入保护（ORM约束） | 特殊字符输入不破坏 SQL 结构 |
| E023 | 安全 | rate limit（若实现）保护 | 高频请求被限流 |
| E024 | 前端 | ProtectedRoute未认证重定向 | 访问受保护页自动跳登录 |
| E025 | 前端 | Session页面错误态提示与返回按钮 | 接口报错时显示友好提示及返回键 |
| E026 | 前端 | Video页面无job提示与返回 | 提示无视频并引导返回 |
| E027 | 前端 | TodayPlan无词书引导选择 | 引导用户去设置页选书 |
| E028 | 前端 | 题目倒计时UI一致性 | 倒计时结束自动提交或提示 |
| E029 | 前端 | 例句挖空视觉验证 | 目标单词被下划线替换 |
| E030 | 前端 | word overlay滚动显示完整 | 单词过多时可横向滚动 |
| E031 | 前端 | 主题色在复习模式与Booster区分 | 视觉上能区分不同模式 |
| E032 | 前端 | 路由导航与状态恢复一致 | 浏览器后退/前进状态同步 |
| E033 | 前端 | 大量options时布局不溢出 | 选项过多时布局自适应 |
| E034 | 前端 | 浏览器兼容（Chrome/Edge）基本渲染 | 主流浏览器显示正常 |

## 4. 运维与文档 (E035-E050)

| 用例ID | 模块 | 用例标题 | 预期结果 |
| :--- | :--- | :--- | :--- |
| E035 | 部署 | docker-compose down/up后数据持久 | 数据库数据不丢失 |
| E036 | 部署 | 重启数据库后服务健康恢复 | 后端自动重连数据库 |
| E037 | 部署 | 端口占用冲突提示与解决 | 启动失败并提示端口占用 |
| E038 | 部署 | 环境变量缺失时默认路径工作 | 使用代码默认配置 |
| E039 | 日志 | 关键操作日志具备可追踪性 | 包含 userId, action 等关键信息 |
| E040 | 可观察 | 健康检查/health正常返回 | 返回 200 OK |
| E041 | i18n | 提示文案中文/英文一致性（如有） | 语言风格统一 |
| E042 | 辅助 | 生词本API后端存在但前端无入口 | 接口可调用 (作为预留功能) |
| E043 | 辅助 | 发音字段存在但前端不播放 | 数据存在 (作为预留功能) |
| E044 | 辅助 | CheckInCalendar存在（前端）展示 | 日历组件渲染正常 |
| E045 | 文档 | 部署文档步骤完整可执行 | 照文档操作可成功部署 |
| E046 | 文档 | 故障排查指南内容覆盖关键问题 | 包含常见报错解决方案 |
| E047 | 文档 | 安全建议文档未误导用户 | 建议合理可行 |
| E048 | 文档 | API文档与实际返回一致 | 接口字段与文档描述符 |
| E049 | 需求 | PRD与实现差异点记录 | 差异有据可查 |
| E050 | 维护 | 版本记录与变更说明一致性 | CHANGELOG 更新及时 |

## 5. 深水区测试 (E051-E057)

| 用例ID | 模块 | 用例标题 | 预期结果 |
| :--- | :--- | :--- | :--- |
| E051 | 边界 | 跨日临界点提交 | 23:59:59 提交的进度归属正确 (通常按提交时间) |
| E052 | 边界 | 时区变更 | 服务器 UTC 与用户 UTC+8 的每日刷新逻辑一致 |
| E053 | 并发 | 多端同时学习同一词 | 最终状态一致，无数据损坏 (Last Write Wins 或 乐观锁) |
| E054 | 并发 | 视频生成任务的幂等性 | 短时间多次点击仅生成一个任务 |
| E055 | 资源 | 磁盘满载下的视频生成 | 脚本写入失败时 Job 标记为 Failed，后端不崩溃 |
| E056 | 资源 | 僵尸进程检测 | 脚本异常退出无日志时，任务超时标记 Failed |
| E057 | 网络 | 提交答案时的网络闪断 | 前端提示重试，后端处理重复提交 (幂等) |

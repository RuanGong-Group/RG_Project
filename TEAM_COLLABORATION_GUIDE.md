# 语境记忆项目团队协作与Git工作流指南 

**版本**: 1.0
**日期**: 2025年10月31日
**制定者**: GitHub Copilot

---

## 第一部分：核心原则与分支模型

本指南旨在为“语境记忆”项目冲刺发布阶段提供清晰、高效的团队协作流程。我们将采用以测试和发布为核心的 **GitHub Flow 简化版** 模型。

### 1.1 分支定义与职责

| 分支类型 | 命名规则 | **核心职责** | 谁来操作？ |
| :--- | :--- | :--- | :--- |
| `main` | 固定 | **生产分支**：存放最稳定、可随时发布给用户的代码。**严格受保护**。 | 组长 (合并), 配置管理员 (维护) |
| `dev` | 固定 | **开发分支**：所有修复、优化、新功能的“集合点”，是**测试经理的主战场**。 | 全员 (合并任务分支), 测试经理 (测试) |
| `feature/*` | `feature/ai-func` | **功能分支**：从 `dev` 分出，用于**个人独立开发**。一个分支只做一件事。 | 架构师, 普通开发者 |
| `hotfix/*` | `hotfix/crash-bug` | **热修复分支**：从 `main` 分出，用于修复**线上生产环境的紧急、严重Bug**。 | 架构师, 组长 (批准) |

### 1.2 核心工作流图

```mermaid
graph TD
    subgraph "生产环境 (Production)"
        main[main (v1.0)]
    end

    subgraph "集成与测试环境 (Staging)"
        dev[dev]
    end

    subgraph "个人开发环境 (Local)"
        feature_branch[feature 分支]
    end
    
    subgraph "紧急修复 (Emergency)"
        hotfix_branch[hotfix 分支]
    end

    feature_branch -- 1. 提交PR (Pull Request) --> dev
    dev -- 2. 测试通过后, 提交发布PR --> main
    main -- 3. 发现线上紧急Bug --> hotfix_branch
    hotfix_branch -- 4. 修复后提交PR --> main
    hotfix_branch -- 5. 同时提交PR同步 --> dev
```

**解释**:
- **生产环境 (Production)**: `main` 分支是生产环境的代码，始终保持稳定，只有经过严格测试的代码才能合并到这里。
- **集成与测试环境 (Staging)**: `dev` 分支是开发的集成点，所有功能分支的代码会先合并到这里进行测试。
- **个人开发环境 (Local)**: `feature/*` 分支是开发者个人的工作区，用于开发具体功能或修复问题。
- **紧急修复 (Emergency)**: `hotfix/*` 分支用于修复生产环境中的紧急问题，修复完成后会同步到 `main` 和 `dev` 分支。

**关键规则**: **任何代码在合并到 `main` 分支前，必须先在 `dev` 分支经过测试经理的完整测试。**

---

## 第二部分：角色分工与职责

### 2.1 组长 
- **核心职责**: 项目总负责人，对最终产品质量和发布节奏负责。
- **具体任务**:
    1.  **任务规划与分配**: 在GitHub Issues中创建任务，并指派给团队成员。
    2.  **决策审批**:
        - **批准发布**: 审批从 `dev` 到 `main` 的发布Pull Request。这是发布新版本的唯一入口。
        - **批准热修复**: 审批 `hotfix` 分支到 `main` 的紧急修复PR。
    3.  **进度跟踪**: 主持每日站会和每周复盘，确保项目按计划进行。
    4.  **冲突解决**: 解决团队内部的技术分歧或资源冲突。

### 2.2 配置管理员
- **核心职责**: 保障开发环境的一致性、仓库的规范性和自动化流程的顺畅。
- **具体任务**:
    1.  **仓库初始化与维护**:
        - 负责首次 `git init`，创建 `.gitignore`，关联远程仓库。
        - 在GitHub/Gitee上设置 `main` 分支的**保护规则**。
    2.  **环境管理**:
        - 维护 `backend/.env.example` 和 `frontend_v3/.env.example` 文件，确保所有必需的环境变量都有说明。
        - 编写并维护项目的《环境配置指南》。
    3.  **依赖管理**: 定期检查并更新 `package.json` 中的依赖，处理潜在的安全漏洞。
    4.  **(未来)** **CI/CD建设**: 搭建自动化测试和部署流水线（例如使用 GitHub Actions）。

### 2.3 架构师
- **核心职责**: 技术方案的制定者和代码质量的把关人。
- **具体任务**:
    1.  **技术攻坚**: 负责复杂任务的开发，如“AI功能集成”、“生词本核心逻辑”等。
    2.  **代码审查 (Code Review)**:
        - 作为所有Pull Request的主要审查者。
        - 确保新代码符合 `projectrules.md` 规范和整体架构设计。
        - 拒绝不合格的代码，并给出清晰的修改建议。
    3.  **分支开发**:
        - 从 `dev` 创建任务分支 (e.g., `feature/ai-engine`)。
        - 开发完成后，提交PR到 `dev`，并清晰描述改动。
    4.  **热修复执行**: 当出现线上紧急Bug时，由架构师负责从 `main` 创建 `hotfix` 分支进行快速修复。

### 2.4 测试经理
- **核心职责**: 软件质量的“守门员”，确保交付给用户的产品没有严重Bug。
- **具体任务**:
    1.  **测试用例编写**: 根据 `PRD.md` 和 `TESTING_ISSUES_FIXED.md`，编写并维护一份完整的《端到端测试用例》清单。
    2.  **`dev` 分支测试**:
        - **这是您的核心工作区**。一旦有新的 `fix` 或 `feature` 分支合并进来，您就需要将 `dev` 分支的代码部署到测试环境。
        - 执行完整的端到端测试，确保新功能符合预期且没有破坏旧功能（回归测试）。
    3.  **Bug报告**: 在GitHub Issues中创建详细的Bug报告，清晰描述复现步骤、期望结果和实际结果，并指派给架构师或相关开发者。
    4.  **发布确认**: 当且仅当 `dev` 分支通过所有测试用例后，向**组长**发出“可以发布”的确认信号。

---

## 第三部分：详细协作流程

### 场景一：常规开发流程

1.  **组长**: 在 GitHub Issues 中分配任务。
2.  **开发者**: 从 `dev` 分支创建新的 `feature` 分支进行开发。
3.  **开发者**: 完成开发后，向 `dev` 分支发起 Pull Request (PR)。
4.  **架构师/审查者**: 对 PR 进行代码审查 (Code Review)，提出修改意见或批准。
5.  **开发者**: 根据审查意见修改代码。
6.  **架构师/审查者**: 批准并合并 PR 到 `dev` 分支。
7.  **测试经理**: 在 `dev` 分支上对新合并的功能进行测试。

### 场景二：版本发布流程

1.  **测试经理**: 在 `dev` 分支上完成所有功能的全面测试，确认版本达到发布标准。
2.  **测试经理**: 向组长发出“可以发布”的确认信号。
3.  **组长/配置管理员**: 创建一个从 `dev` 到 `main` 的发布 PR。
4.  **组长**: 进行最终审批，并合并 PR 到 `main` 分支。
5.  **配置管理员**: 为 `main` 分支的这次提交打上版本标签 (如 `v1.1.0`) 并推送到远程仓库。

### 场景三：紧急热修复流程

1.  **团队成员**: 发现线上 `main` 分支的紧急 Bug。
2.  **架构师**: 立即从 `main` 分支创建 `hotfix` 分支进行修复。
3.  **架构师**: 修复完成后，同时向 `main` 和 `dev` 分支发起两个独立的 PR。
4.  **组长**: 优先审查并合并到 `main` 分支的 PR，用于紧急上线。
5.  **配置管理员**: 为 `main` 分支打上热修复版本标签 (如 `v1.1.1`) 并部署。
6.  **架构师/审查者**: 随后审查并合并到 `dev` 分支的 PR，确保代码同步。

### 场景四：处理合并冲突

1.  **PR 发起者（开发者）**: 在提交从 `feature` 分支到 `dev` 分支的 Pull Request 时，GitHub 提示存在合并冲突。
2.  **PR 发起者（开发者）**: 在本地将远程 `dev` 分支的最新代码同步到自己的 `feature` 分支。
3.  **PR 发起者（开发者）**: 在本地手动编辑并解决所有代码冲突。
4.  **PR 发起者（开发者）**: 提交解决冲突后的代码，并再次推送到自己的远程 `feature` 分支。
5.  **GitHub**: 自动更新 Pull Request，冲突标记消失。
6.  **架构师/审查者**: 继续进行代码审查和合并。

### 场景五：每日与开发分支同步

1.  **开发者**: 每天开始工作前，将远程 `dev` 分支的最新代码同步到本地 `dev` 分支。
2.  **开发者**: 将本地 `dev` 分支的最新代码合并到自己正在开发的 `feature` 分支。
3.  **开发者**: 如果出现冲突，立即在本地解决（参考场景四）。
4.  **开发者**: 继续在已同步的 `feature` 分支上开发。
5.  **目的**: 避免在最后提交 PR 时产生巨大的合并冲突，保持"小步快跑、持续集成"的开发节奏。

### 场景六：代码审查与反馈循环

1.  **PR 发起者**: 向 `dev` 分支提交 Pull Request。
2.  **架构师/审查者**: 审查代码，在 GitHub 的 PR 页面中针对具体代码行提出修改意见或疑问。
3.  **PR 发起者**: 收到审查反馈后，在本地 `feature` 分支上进行修改。
4.  **PR 发起者**: 提交修改后的代码并推送，GitHub 会自动更新该 PR。
5.  **架构师/审查者**: 重新审查修改后的代码，确认问题已解决。
6.  **架构师/审查者**: 批准 PR 并合并到 `dev` 分支。
7.  **目的**: 通过多轮反馈循环，确保代码质量符合团队标准。

---

## 第四部分：GitHub 仓库与规范配置

为确保以上流程得以严格执行，配置管理员需要在 GitHub 上进行以下配置：

### 4.1 分支保护规则

配置管理员需要在 GitHub 仓库的 `Settings > Branches` 中设置以下分支保护规则：

#### 1. `main` 分支保护规则

- **目标**: 确保 `main` 分支的绝对稳定，所有代码必须经过 `dev` 分支的完整测试。
- **具体配置**:
    1.  **Require a pull request before merging**: **必须启用**。
        - 禁止直接向 `main` 分支推送代码，所有合并都必须通过 PR。
    2.  **Require approvals**: **建议启用 (至少1人)**。
        - PR 需要至少一名指定人员（如组长）批准后才能合并。
    3.  **Restrict who can push to matching branches**: **建议启用**。
        - 仅允许配置管理员等少数人拥有直接推送权限（用于紧急情况）。

#### 2. `dev` 分支保护规则

- **目标**: 确保进入 `dev` 分支的代码都经过了至少一次审查，保证集成测试环境的相对稳定。
- **具体配置**:
    1.  **Require a pull request before merging**: **必须启用**。
        - 禁止直接向 `dev` 分支推送代码，所有功能开发都必须在 `feature` 分支完成。
    2.  **Require approvals**: **建议启用 (至少1人)**。
        - PR 需要至少一名指定人员（如架构师）批准后才能合并。

#### 3. `feature/*` 和 `hotfix/*` 分支

- **目标**: 为开发者提供灵活的开发空间。
- **具体配置**:
    - **无需设置保护规则**。开发者可以自由地向自己的 `feature` 或 `hotfix` 分支推送代码。

---

### 4.2 提交信息规范 (Commit Message Convention)

为了让项目历史清晰易读，团队应遵循统一的提交信息格式。推荐使用 **Conventional Commits** 规范：

**格式**: `<类型>(<分支类型>): <简短描述>`

**常用类型**:
- `feat`: 新增功能
- `fix`: 修复 Bug
- `docs`: 文档更新
- `style`: 代码格式调整（不影响功能）
- `refactor`: 代码重构（不新增功能，也不修复 Bug）
- `test`: 新增或修改测试
- `chore`: 构建工具或辅助工具的变动

**分支类型说明**:
- 在 `feature` 分支上提交时，使用 `feature`
- 在 `hotfix` 分支上提交时，使用 `hotfix`
- 在 `dev` 或 `main` 分支上提交时，可省略分支类型（通常不应直接在这些分支上提交）

**示例**:
```
feat(feature): 添加用户登录功能
fix(feature): 修复统计页面图表不显示的问题
docs(main): 更新环境配置指南
refactor(feature): 重构学习记录模块
fix(hotfix): 修复线上登录闪退问题
```

**执行方式**: 这是一种"软约束"，依靠团队成员自律和代码审查时的提醒来执行。

---

### 4.3 Pull Request 模板

以下是团队统一的PR模板，开发者在提交PR时需遵循：

```markdown
## 变更类型
- [ ] 新功能 (feat)
- [ ] Bug 修复 (fix)
- [ ] 代码重构 (refactor)
- [ ] 文档更新 (docs)
- [ ] 其他

## 变更描述
请简要描述这次 PR 的主要变更内容。

## 相关 Issue
关联的 Issue 编号（如有）：#

## 测试说明
请说明如何验证这次变更，或已进行的测试。

## 检查清单
- [ ] 代码符合项目规范 (参考 projectrules.md)
- [ ] 已在本地测试，功能正常
- [ ] 已更新相关文档（如有必要）
```

### 4.4 Issue 模板

以下是团队统一的Issue模板，用于报告Bug或提出功能请求：

#### Bug 报告模板
```markdown
## Bug 描述
简要描述遇到的问题。

## 复现步骤
1. 进入...页面
2. 点击...按钮
3. 观察到...错误

## 期望行为
描述你期望看到的正确行为。

## 实际行为
描述实际发生的错误行为。

## 环境信息
- 浏览器/设备：
- 操作系统：
- 应用版本：
```



### 4.5 什么时候需要发布PR

Pull Request (PR) 是团队协作的核心工具，用于代码审查、功能集成和版本发布。以下是需要发布PR的典型场景：

1. **功能开发完成**:
   - 当您在 `feature/*` 分支上完成了一个功能开发，需要将代码合并到 `dev` 分支时。
   - PR 应包含清晰的变更描述和测试说明。

2. **修复Bug**:
   - 当您在 `feature/*` 或 `hotfix/*` 分支上完成了一个Bug修复，需要将代码合并到 `dev` 或 `main` 分支时。
   - 如果是紧急修复，需优先提交到 `main`，并同步到 `dev`。

3. **代码重构**:
   - 当您对代码进行了重构（不新增功能或修复Bug），需要将代码合并到 `dev` 分支时。
   - PR 应说明重构的目的和范围。

4. **文档更新**:
   - 当您更新了项目文档（如 `README.md` 或 `TEAM_COLLABORATION_GUIDE.md`），需要将变更合并到 `dev` 或 `main` 分支时。

5. **版本发布**:
   - 当 `dev` 分支的所有功能和修复通过测试后，需要将代码合并到 `main` 分支发布新版本时。
   - PR 应包含版本号和发布说明。

**注意**: 每个PR都应关联到相关的Issue（如果有），并确保通过所有必要的代码审查和测试。

### 4.6分支合并时间规定（固定发布窗口）
1. **核心规则：**
- 每周六 20:00 - 22:00 为固定的“合并窗口期”。
- 所有计划在本周集成的 feature/*, fix/*, polish/*分支，应在此时间窗口内合并到 develop分支。
- 非紧急的PR，建议在此时间之前完成代码审查，并在窗口期内统一合并。

2. **工作流程：**
- 周六前：开发者完成功能开发，提交PR，并完成代码审查讨论。
- 周六 20:00：测试经理确认 develop分支当前状态稳定。
- 合并窗口期：组长或配置管理员将已审核通过的PR依次合并到 develop分支。
- 合并后：测试经理立即对合并后的 develop分支进行集成测试。

3. **例外情况：**
紧急Bug修复：fix/*分支如修复的是阻碍开发或测试的严重Bug，经组长和测试经理同意后，可随时合并。
热修复：hotfix/*分支不受此限制，需立即处理。



这份指南为您和您的团队提供了清晰的路线图。建议将此文件存放在项目根目录，作为团队的行为准则。
